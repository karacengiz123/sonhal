<form method="post" action="{{ path('realtime_queues_delete', {'name': realtime_queue.name}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ realtime_queue.name) }}">
    <button class="btn">Delete</button>
</form>

{% extends 'base.html.twig' %}

{% block title %}Report{% endblock %}

{% block body %}
    <h1>Report</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ report.id }}</td>
            </tr>
            <tr>
                <th>Firstdate</th>
                <td>{{ report.firstdate ? report.firstdate|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Lastdate</th>
                <td>{{ report.lastdate ? report.lastdate|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('report_index') }}">back to list</a>

    <a href="{{ path('report_edit', {'id': report.id}) }}">edit</a>

    {{ include('report/_delete_form.html.twig') }}
{% endblock %}

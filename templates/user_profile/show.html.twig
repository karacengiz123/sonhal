{% extends 'base.html.twig' %}

{% block title %}UserProfile{% endblock %}

{% block body %}
    <h1>UserProfile</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ user_profile.id }}</td>
            </tr>
            <tr>
                <th>FirstName</th>
                <td>{{ user_profile.firstName }}</td>
            </tr>
            <tr>
                <th>LastName</th>
                <td>{{ user_profile.lastName }}</td>
            </tr>
            <tr>
                <th>Tckn</th>
                <td>{{ user_profile.tckn }}</td>
            </tr>
            <tr>
                <th>Extension</th>
                <td>{{ user_profile.extension }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('user_profile_index') }}">back to list</a>

    <a href="{{ path('user_profile_edit', {'id': user_profile.id}) }}">edit</a>

    {{ include('user_profile/_delete_form.html.twig') }}
{% endblock %}

{% extends 'base.html.twig' %}

{% block title %}FormQuestion{% endblock %}

{% block body %}
    <h1>FormQuestion</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ form_question.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ form_question.title }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('form_question_index') }}">back to list</a>

    <a href="{{ path('form_question_edit', {'id': form_question.id}) }}">edit</a>

    {{ include('form_question/_delete_form.html.twig') }}
{% endblock %}

{% extends 'base.html.twig' %}

{% block title %}Greeting{% endblock %}

{% block body %}
    <h1>Greeting</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ greeting.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ greeting.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('greeting_index') }}">back to list</a>

    <a href="{{ path('greeting_edit', {'id': greeting.id}) }}">edit</a>

    {{ include('greeting/_delete_form.html.twig') }}
{% endblock %}

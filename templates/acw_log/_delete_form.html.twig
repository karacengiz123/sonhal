<form method="post" action="{{ path('acw_log_delete', {'id': acw_log.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ acw_log.id) }}">
    <button class="btn">Delete</button>
</form>

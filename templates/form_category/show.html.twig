{% extends 'base.html.twig' %}

{% block title %}FormCategory{% endblock %}

{% block body %}
    <h1>FormCategory</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ form_category.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ form_category.title }}</td>
            </tr>
            <tr>
                <th>MinScore</th>
                <td>{{ form_category.minScore }}</td>
            </tr>
            <tr>
                <th>Sort</th>
                <td>{{ form_category.sort }}</td>
            </tr>
            <tr>
                <th>MaxScore</th>
                <td>{{ form_category.maxScore }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('form_category_index') }}">back to list</a>

    <a href="{{ path('form_category_edit', {'id': form_category.id}) }}">edit</a>

    {{ include('form_category/_delete_form.html.twig') }}
{% endblock %}

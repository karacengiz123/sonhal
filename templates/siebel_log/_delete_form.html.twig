<form method="post" action="{{ path('siebel_log_delete', {'id': siebel_log.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ siebel_log.id) }}">
    <button class="btn">Delete</button>
</form>

<form method="post" action="{{ path('form_question_option_delete', {'id': form_question_option.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ form_question_option.id) }}">
    <button class="btn">Delete</button>
</form>

{% extends 'base.html.twig' %}

{% block title %}FormSection{% endblock %}

{% block body %}
    <h1>FormSection</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ form_section.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ form_section.title }}</td>
            </tr>
            <tr>
                <th>MinScore</th>
                <td>{{ form_section.minScore }}</td>
            </tr>
            <tr>
                <th>Sort</th>
                <td>{{ form_section.sort }}</td>
            </tr>
            <tr>
                <th>MaxScore</th>
                <td>{{ form_section.maxScore }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('form_section_index') }}">back to list</a>

    <a href="{{ path('form_section_edit', {'id': form_section.id}) }}">edit</a>

    {{ include('form_section/_delete_form.html.twig') }}
{% endblock %}

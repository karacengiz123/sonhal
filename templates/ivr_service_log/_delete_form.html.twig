<form method="post" action="{{ path('ivr_service_log_delete', {'id': ivr_service_log.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ivr_service_log.id) }}">
    <button class="btn">Delete</button>
</form>

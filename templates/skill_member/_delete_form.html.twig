<form method="post" action="{{ path('skill_member_delete', {'id': skill_member.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ skill_member.id) }}">
    <button class="btn">Delete</button>
</form>

{% extends 'base.html.twig' %}

{% block title %}EvaluationResetReason{% endblock %}

{% block body %}
    <h1>EvaluationResetReason</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ evaluation_reset_reason.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ evaluation_reset_reason.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('evaluation_reset_reason_index') }}">back to list</a>

    <a href="{{ path('evaluation_reset_reason_edit', {'id': evaluation_reset_reason.id}) }}">edit</a>

    {{ include('evaluation_reset_reason/_delete_form.html.twig') }}
{% endblock %}

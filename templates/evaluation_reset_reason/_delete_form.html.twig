<form method="post" action="{{ path('evaluation_reset_reason_delete', {'id': evaluation_reset_reason.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ evaluation_reset_reason.id) }}">
    <button class="btn">Delete</button>
</form>

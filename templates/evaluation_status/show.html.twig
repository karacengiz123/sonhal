{% extends 'base.html.twig' %}

{% block title %}EvaluationStatus{% endblock %}

{% block body %}
    <h1>EvaluationStatus</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ evaluation_status.id }}</td>
            </tr>
            <tr>
                <th>StatusCode</th>
                <td>{{ evaluation_status.statusCode }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ evaluation_status.name }}</td>
            </tr>
            <tr>
                <th>IsActive</th>
                <td>{{ evaluation_status.isActive ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('evaluation_status_index') }}">back to list</a>

    <a href="{{ path('evaluation_status_edit', {'id': evaluation_status.id}) }}">edit</a>

    {{ include('evaluation_status/_delete_form.html.twig') }}
{% endblock %}

<!DOCTYPE html>

<title>WallMessages</title>

{% block body %}
    <h1>WallMessages</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ wall_message.id }}</td>
            </tr>
            <tr>
                <th>Wallmessages</th>
                <td>{{ wall_message.wallmessages }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('wall_messages_index') }}">back to list</a>

    <a href="{{ path('wall_messages_edit', {'id': wall_message.id}) }}">edit</a>

    {{ include('wall_messages/_delete_form.html.twig') }}
{% endblock %}

{% extends 'base.html.twig' %}

{% block title %}EvaluationReasonType index{% endblock %}

{% block body %}
    <h1>EvaluationReasonType index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for evaluation_reason_type in evaluation_reason_types %}
            <tr>
                <td>{{ evaluation_reason_type.id }}</td>
                <td>{{ evaluation_reason_type.name }}</td>
                <td>
                    <a href="{{ path('evaluation_reason_type_show', {'id': evaluation_reason_type.id}) }}">show</a>
                    <a href="{{ path('evaluation_reason_type_edit', {'id': evaluation_reason_type.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('evaluation_reason_type_new') }}">Create new</a>
{% endblock %}

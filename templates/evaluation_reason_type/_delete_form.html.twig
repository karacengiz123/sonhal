<form method="post" action="{{ path('evaluation_reason_type_delete', {'id': evaluation_reason_type.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ evaluation_reason_type.id) }}">
    <button class="btn">Delete</button>
</form>

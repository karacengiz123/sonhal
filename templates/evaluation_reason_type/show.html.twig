{% extends 'base.html.twig' %}

{% block title %}EvaluationReasonType{% endblock %}

{% block body %}
    <h1>EvaluationReasonType</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ evaluation_reason_type.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ evaluation_reason_type.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('evaluation_reason_type_index') }}">back to list</a>

    <a href="{{ path('evaluation_reason_type_edit', {'id': evaluation_reason_type.id}) }}">edit</a>

    {{ include('evaluation_reason_type/_delete_form.html.twig') }}
{% endblock %}

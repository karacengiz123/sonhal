<form method="post" action="{{ path('guide_group_delete', {'id': guide_group.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ guide_group.id) }}">
    <button class="btn btn-danger">Sil</button>
</form>

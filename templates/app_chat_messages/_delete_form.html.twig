<form method="post" action="{{ path('app_chat_messages_delete', {'id': app_chat_message.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ app_chat_message.id) }}">
    <button class="btn">Delete</button>
</form>

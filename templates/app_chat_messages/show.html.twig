{% extends 'base.html.twig' %}

{% block title %}AppChatMessages{% endblock %}

{% block body %}
    <h1>AppChatMessages</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ app_chat_message.id }}</td>
            </tr>
            <tr>
                <th>Sender</th>
                <td>{{ app_chat_message.sender }}</td>
            </tr>
            <tr>
                <th>Message</th>
                <td>{{ app_chat_message.message }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_chat_messages_index') }}">back to list</a>

    <a href="{{ path('app_chat_messages_edit', {'id': app_chat_message.id}) }}">edit</a>

    {{ include('app_chat_messages/_delete_form.html.twig') }}
{% endblock %}

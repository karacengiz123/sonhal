<form method="post" action="{{ path('agent_break_delete', {'id': agent_break.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ agent_break.id) }}">
    <button class="btn">Delete</button>
</form>
